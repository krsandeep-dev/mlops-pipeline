mlflow==3.15.1
boto3==1.43.56
dvc[s3]==3.67.1

# Filtered from Airflow's constraints (they conflict with mlflow's ceilings);
# re-pinned here by hand. Keep in sync with uv.lock.
pandas==2.3.3
cryptography==49.0.0
pyarrow==25.0.0

# Also filtered, for a different reason — see the Dockerfile comment. The host is
# pinned to Python 3.11 and numpy 2.5 / scipy 1.18 require >=3.12, so the host
# cannot reach the constrained versions. Pinned down to match uv.lock instead.
numpy==2.4.6
scipy==1.17.1
