mlflow==3.15.1
boto3==1.43.56
dvc[s3]==3.67.1

# Filtered from Airflow's constraints (they conflict with mlflow's ceilings);
# re-pinned here by hand. Keep in sync with uv.lock.
pandas==2.3.3
cryptography==49.0.0
pyarrow==25.0.0

# Training runs in-network from 2.3 (the host cannot resolve presigned artifact
# URLs, which are minted with the internal minio hostname). scikit-learn and
# narwhals are already pinned by the 3.11 constraints at the versions uv.lock
# holds, so only lightgbm needs an explicit pin.
lightgbm==4.7.0

# Batch drift. Resolves cleanly inside Airflow's own constraints -- it is not pinned there
# at all, and it moves nothing: numpy, scipy, scikit-learn and pandas all stay where the
# image already had them. The spec's fallback to an isolated image is not needed.
evidently==0.7.23
