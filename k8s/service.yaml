apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: serving
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: model-serving
  ports:
    - name: http
      port: 80
      targetPort: http
