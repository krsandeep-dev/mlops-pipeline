[project]
name = "mlops-pipeline"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.43.73",
    "mlflow==3.15.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.3",
]
